curl --data "username={{dns.user}}&password={{dns.password}}" https://www.goip.de/setip?   

{% if app_nextcloud.using  %}
curl --data "username={{dns.user}}&password={{dns.password}}&subdomain={{app_nextcloud.host}}" https://www.goip.de/setip?    
{% endif %}

{% if app_bookstack.using  %}
curl --data "username={{dns.user}}&password={{dns.password}}&subdomain={{app_bookstack.host}}" https://www.goip.de/setip?    
{% endif %}
